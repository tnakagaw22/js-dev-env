/*
  This file contains references to the vendor libraries used in this project.
  This is used by webpack in the production build only.
  Vendor libraries are bundled together separately from the projet codes so that it can be cached.
*/

/*eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
